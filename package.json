{
  "name": "news",
  "module": "index.ts",
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "prettier": "^3.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "start": "bun run src/hello.ts",
    "pdf-to-md": "bun run src/pdf-to-md.ts",
    "test": "bun test",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "bun run lint && bun run format:check && bun run test",
    "prepare": "husky"
  },
  "type": "module",
  "dependencies": {
    "pdfjs-dist": "^5.6.205"
  }
}
